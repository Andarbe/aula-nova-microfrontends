import { mount } from './mount'
// Autonomia: se ejecuta solo, con un curso de ejemplo precargado.
mount(document.getElementById('root')!, { courseId: 'c3', courseTitle: 'Arquitectura Front-End Moderna' })
